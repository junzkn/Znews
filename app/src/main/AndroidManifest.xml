<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jun.znews">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".ThisApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.WelcomeActivity"
            android:theme="@style/WelcomeTheme"></activity>
        <activity
            android:name=".ui.news.content.ArticleReadActivity"
            android:theme="@style/BGA"></activity>
        <activity
            android:name=".ui.news.content.AdvertReadActivity"
            android:theme="@style/BGA"></activity>
        <activity android:name=".ui.news.content.ImageBrowseActivity"
            android:theme="@style/BGA"></activity>
    </application>

</manifest>